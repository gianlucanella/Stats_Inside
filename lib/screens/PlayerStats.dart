import 'package:flutter/material.dart';

class PlayerStatsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Statistiche Giocatori'),
      ),
      body: Center(
        child: Text('Statistiche Giocatori'),
      ),
    );
  }
}
